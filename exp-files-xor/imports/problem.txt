$fitnessType = avgLastBoolScore

$qFunctions = f.allNamed(fs = m.sKeyMap(
  keys = [behavior; avgLastBoolScore; increase; consistency];
  values = [
    f.identity();
    ds.e.sBool.avgScore(
      scoreType = boolean;
      caseIndexes = [360:1:399]  
    );
    ds.e.sBool.avgScoreDelta(
      scoreType = unlimited;
      firstIndexes = [-10:1:-1]; 
      secondIndexes = [0:1:1]    
    );
    ds.e.sBool.avgScoreVariation(
      scoreType = unlimited;
      indexes = [-8:1:-1]  
    )
  ]
))

$xorProblem = ea.p.simToTo(
  name = "xor";
  simulation = ds.s.sequentialXor(
    cases = $cases;
    rewardType = unlimited;
    resetAgent = true;
    shuffle = true
  );
  dT = 1;
  tRange = $tRangeLife;
  comparator = ea.comparator.descending(of = f.mapValue(key = avgLastBoolScore));
  qFunction = $qFunctions
)

$caseIndexesParity = [928:1:959]
$parityProblem = ea.p.simToTo(
  name = "parity";
  simulation = ds.s.sequentialBf(
    cases = $casesParity;
    target = ea.misc.evenParityBf(n = 5);
    rewardType = unlimited;
    resetAgent = true
  );
  dT = 1;
  tRange = $tRangeParity;
  comparator = ea.comparator.descending(of = f.mapValue(key = avgLastBoolScore));
  qFunction = $qFunctions
)

$caseIndexesMopm = [1856:1:1919]
$mopmProblem = ea.p.simToTo(
  name = "mopm";
  simulation = ds.s.sequentialBf(
    cases = $casesMopm;
    target = ea.misc.mopmBf(n = 3);
    rewardType = unlimited;
    resetAgent = true
  );
  dT = 1;
  tRange = $tRangeMopm;
  comparator = ea.comparator.descending(of = f.mapValue(key = avgLastBoolScore));
  qFunction = $qFunctions
)