$tRange = m.range(min = 0; max = 110)

$problem = ea.p.simToTo(
  name = "sequentialXor";
  dT = 1;
  tRange = $tRange;
  simulation = ds.s.sequantialXor(
    cases = ["00"];
    rewardType = unlimited
  );
  qFunction = f.allNamed(fs = m.sKeyMap(
    keys = [behavior; avgLastBoolScore];
    values = [
      f.identity();
      ds.e.sxor.avgError(
        rewardType = boolean;
        caseIndexes = [100:1:109]
      )
    ]
  ));
  comparator = ea.comparator.descending(of = f.mapValue(key = avgLastBoolScore))
)